function ollama_call() {
    curl http://localhost:11434/api/chat -d '{
  "model": "llama3",
  "stream": false,
  "messages": [
    { "role": "user", "content": "${1}" }
  ]
}'
}

alias chat='parllama'
